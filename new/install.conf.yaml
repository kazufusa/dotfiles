- defaults:
    link:
      relink: true

- clean: ['~/.config']

- link:
    ~/.config/tmux: config/tmux
    ~/.conig/git: config/git

- shell:
  - [git submodule update --init --recursive, Installing submodules]

